マウント、ファイルシステムの結合。
/ファイルシステムにあるからのディレクトリを使い、階層構造を実現。
/mediaや/mntはデフォルトで用意。

/etc/fstabファイル
ファイルシステムのマウント情報を記載。
マウントの設定ファイル。

blkidコマンドでは、ラベルやUUIDを確認できる。
ラベルとは人間向けの名前。USBメモリに”バックアップ”みたいな感じでつけれる。
UUIDとはファイルシステム専用の識別子。

ファイルシステムをマウントするにはmountコマンドを使う。
オプションや引数ナシで実行すると現在のファイルシステムのマウント状況を確認できる。
-a autoオプションが記載されているファイルシステムをすべてマウント
-t ファイルシステムの種類を指定
-o マウントオプションを指定

lsblkではファイルシステムのマウント状況に絞って確認できる。

ファイルシステムの解除はアンマウント。
umount。
デバイス使用中にアンマウントするとtarget is busyとでる。

lsofでデバイスの使用状況がわかる。
fuserでは引数のディレクトリを、どのPIDプロセスが使用しているかがわかる。

ファイルシステムはiノードを格納するiノード領域と、ファイルの実態を格納するデータ保存領域に分かれている。
空き容量はdfコマンド
-h 人にわかりやすい単位で容量を表示
-k KB単位
-i iノード領域の使用状況を表示

ファイルやディレクトリごとに使用している容量を確認したい場合はduコマンド

/etc/fstabに記述するマウントオプションで、mount -a実行時にマウントするよう指定するものはauto
linuxでは/proc/self/mountsと/proc/mountsファイルに現在マウントされているファイスシステムの情報が格納。
/etc/fstabはマウントの設定ファイル。

mountの-tはファイルシステムの種類を指定する
mount -t iso9660 /dev/hdc /mnt/cdrom

/etc/fstabファイルにエントリが追加済みの場合、どちらか片方だけ指定すればのこりは/etc/fstabを参照して決定される。

以下のように記述があったとする。
UUID=1234-ABCD  /data  ext4  defaults  0  2
左から、デバイス名、マウントポイント、ファイルシステムの種類、マウントオプション、dumpフラグ、fsckフラグ

mount /dataまたは、mount UUID=1234-ABCD のようにデバイス名（UUID）とマウントポイントのどちらか一方だけを指定すればもう一方の情報は/etc/fstabから取得される。

mount マウントするデバイス名 マウントポイント
-tオプションはファイルシステムの種類を指定する。CD-ROMの場合iso9660だが、指定なしの場合自動判別。

blkidはデバイス名やUUID、ファイスシステムのタイプを表示。
lsblkはブロックデバイスをツリー状に一覧表示。
両方ともUUIDを確認できる。

/etc/fstabに記載がなく、systemdにも認識されていないファイルシステムを手動でマウントするときのsystemdの挙動
・systemdが認識していないファイルシステムはマウントすることにより認識
・systemdは/etc/fstabに設定がないファイルシステムでも認識する
再起動後に設定が失われる。自動マウント設定としては扱わない。

systemd
・Unitと呼ばれる処理単位を使いOSの動作モードやサービス状態など、Linux全体の管理を行える仕組みになっている
・Linux起動人関連するのはターゲットUnit
カーネル起動後にsystemdプロセスが起動する。Linux内のプロセスすべてをsystemdプロセスが制御。

umount オプション マウントされているデバイス名またはマウントポイント
-a /etc/mtabに記載されてるファイルシステムすべてをアンマウント
-t 指定した種類のファイルシステムのみをアンマウント
-at xfsオプションを指定するとxfs形式のファイルシステムすべてをアンマウント。
/dev/sdb2を/dataにマウント→アンマウントする
umount /data
umount /dev/sdc2
umount -at xfs

/etc/fstabファイルに記述するマウントオプションで、出入力を非同期で行うものはasync
async 非同期で入出力を行う。

noauto mount -a コマンド実行時にマウントしない
noexec バイナリの実行を禁止
exec バイナリの実行を許可
suid SUID/SGIDを有効化
auto -aコマンド時にマウントする
defaults 「async auto dev exec nouser rw suid」 の各オプションが指定されたことを意味

--bind mount --bind dir1 dir2 dir1をdir2にマウントする。

mount /dev/sdb1 /mt/data
 デバイスのファイルシステムをマウント
mount --bind /dir1 /dir2
 既存ディレクトリを別の場所へ再マウント

例えば、

/dir1
├─ a.txt
└─ b.txt

とあるとき、

mount --bind /dir1 /dir2

とすると、

/dir2
├─ a.txt
└─ b.txt

と見える。


user 一般ユーザーでマウント可、本人のみアンマウント可
users 一般ユーザーでマウント可、誰でもアンマウント可
nouser 一般ユーザーによるマウントを禁止