稼働中のプロセスの制御
# 1:プロセスの確認
プロセスとは実行状態にあるプログラムのことで、実行中のプログラムを管理する基本単位。  
Linuxではプログラムを実行するたびに新しいプロセスが生成され、識別用のIDが付与される。これをプロセスID(PID)と呼ぶ。  

実行中のプロセスを確認するにはpsコマンドを使う。

~~~
macaroni@PC130:~/projects/lpic_lv1/ch.5$ ps
    PID TTY          TIME CMD
  36000 pts/5    00:00:00 bash
  72914 pts/5    00:00:00 ps
~~~

TTYはプロセスが実行されている画面・端末を表す。   
Linuxがインストールされたハードウェアに直接接続されているディスプレイはtty、ネットワーク越しに接続している場合はpts。

TIMEはプロセスが実際にCPUを使用した時間を表す。

CMDは、実行されているコマンド・プロセスの名称。

# 2:システムの稼働状況の確認
- uptime  
Linuxの起動時刻、トータルの稼働時間、ログイン中のユーザー数、平均不可が表示される。
~~~
macaroni@PC130:~/projects/lpic_lv1/ch.5$ uptime
 15:01:12 up 13:20,  1 user,  load average: 0.04, 0.04, 0.00
~~~
- free  
メモリの使用状況を表示。
~~~
macaroni@PC130:~/projects/lpic_lv1/ch.5$ free
               total        used        free      shared  buff/cache   available
Mem:         7971040     1159768     6778640        4704      204508     6811272
Swap:        2097152           0     2097152
~~~
- uname   
カーネルのバージョン等を確認。
~~~
macaroni@PC130:~/projects/lpic_lv1/ch.5$ uname -a
Linux PC130 6.6.87.2-microsoft-standard-WSL2 #1 SMP PREEMPT_DYNAMIC Thu Jun  5 18:30:46 UTC 2025 x86_64 x86_64 x86_64 GNU/Linux

macaroni@PC130:~/projects/lpic_lv1/ch.5$ uname -r
6.6.87.2-microsoft-standard-WSL2
~~~
- watch   
指定したコマンドを一定時間ごとに繰り返し実行できる。

~~~
# 2秒おきにuptimeコマンドを実行
macaroni@PC130:~/projects/lpic_lv1/ch.5$ watch -n 2 -d uptime
~~~

# 3:プロセスの終了
プロセスを終了する場合、killコマンドでシグナルと呼ばれる信号をプロセスへ送る。

- tmuxコマンド
1つの画面を分割できる。   
ペイン…実行環境
ウィンドウ…操作画面

# 4:プロセスの優先順位
~~~
macaroni@MYCOMPUTER:~/projects/lpic_lv1$ ps -l
F S   UID     PID    PPID  C PRI  NI ADDR SZ WCHAN  TTY          TIME CMD
0 S  1000     676     652  0  80   0 -  1552 do_wai pts/6    00:00:00 bash
0 R  1000    6925     676  0  80   0 -  2078 -      pts/6    00:00:00 ps
~~~
PRIが優先度。LinuxではPRIを直接変更できない仕様になっているため、PRIに影響を与える値をしてnice値が用意されている(NI)  
NI値をPRIに足す。PRIの数値が低いプロセスほど優先される。nice値をマイナスに設定できるのはrootのみ。

# 5:ジョブの制御
シェル上で実行される、コマンドやプログラムなどの処理のまとまりをジョブという。

フォアグラウンドジョブ…実行中に端末を専有するジョブ
通常通り、コマンドを入力しエンター。

バックグラウンドジョブ…実行中に端末を専有しないジョブ
コマンドの末尾に＆記号を追加しエンター。
~~~
macaroni@MYCOMPUTER:~/projects/lpic_lv1$ sleep 10 &
[1] 11979
~~~

[1]…ジョブ番号
11979…PID

ジョブはまだ実行中だが、プロンプトが表示されているので別のコマンドを実行できる。

# ぷろせす
プロセスとは実行状態にあるプログラムのこと。
実行中のプログラムを管理する基本単位。
プログラムを実行→新しいプロセス生成、識別用ID付与（プロセスID、PID）

psコマンドで実行中のプロセスを確認する。

a ほかのユーザーのプロセスも表示
f プロセスの親子関係を表示
u プロセスを起動・所有しているユーザー名を表示
x 制御端末のないデーモンなどのプロセスも表示
-e          すべてのプロセスを表示
-f          プロセス情報をフルフォーマットで表示
-l          詳細な情報を表示
-p PID      指定したPIDのプロセス情報のみ表示
-C プロセス名 指定した名前のプロセスのみ表示

UID          PID    PPID  C STIME TTY          TIME CMD
macaroni   14563   14342  0 11:57 pts/4    00:00:00 ps -f

ppidの列は、そのプロセスの親プロセスのPIDを示す。
起動元を親プロセス、起動されたプロセスを子プロセス。

親子関係を表すコマンドにpstreeもある。

pidだけを検索したいときはpgrepコマンド

topコマンドは、端末を専有し、現在のシステムやプロセスの状況などを継続的に監視。

topコマンドの１行目、稼働状況のみを確認するコマンド、uptimeコマンド。

free     メモリの使用状況
uname    OSやホスト名、カーネルのversionを確認
watch    指定したコマンドを一定時間ごとに繰り返し実行
kill     プロセスを終了
tmux     1つの画面を分割
sleep    指定した秒数だけ停止
killall  killがPIDでプロセスを指定するのに対し、killall/pkillはプロセス名を指定
pkill    

nice     
nice -n nice値 コマンド
コマンドを実行する際にnice値を設定する場合。
niceコマンドを使用しない場合はnice値は0、nice値を指定しない場合はnice値は10。

renice   
既に実行されているコマンドのnice値を変更する場合。

コマンドの末尾に&を付けると、バックグラウンドジョブとなる。

jobs     実行中のバックグラウンドジョブを確認
bg       ctrl + zで一時停止シグナルを送った停止中のジョブに対してbgコマンドを実行すると、ジョブがバックグランドで実行される
fg       バックグランドジョブをフォアグランドへ遷移させる
nohup    シェルを終了させてもジョブを実行させ続けたい場合に使う。SIGHUPシグナルを無視してコマンドを実行し続ける。親プロセスであるログインシェルを終了させてもそのプロセスが停止しない。

SIGHUP  1 端末の終了
   INT  2 ctrl + c
   KILL 9 プロセスの強制終了
   TERM 15 プロセスの正常終了 killコマンドのデフォルト
   CONT 18 停止しているプロセスの再開
   STOP 19 プロセスの一時停止
   TSTP 20 キーボードからの一時停止、ctrl + z

killakkはプロセス名を指定してプロセスを終了。
killall プロセス名
オプションを指定して任意のシグナルを送信可能。
killall -15 プロセス名
killall -TERM プロセス名
killall -SIGTERM プロセス名
killall -s 15 プロセス名
killall -s TERM プロセス名
killall -s SIGTERM プロセス名

HUP
シグナルID：1
設定ファイルの再読み込み。
端末切断通知やデーモンの設定再読み込み。

INT
2
実行中プログラムの中断。
ctrl + cで送られる。

KILL
9
即終了。
後始末できない、シグナルハンドラで捕捉できない、無視できない。
補足できないシグナルはkillとstop。

TERM
15
終了してほしいというお願い。
プロセスを後始末できる。
普通はまずtermを送る。
killコマンドでシグナルを指定しない場合に送られるシグナルでもある。

以下同じ意味。
kill 1865
kill -15 1865
kill -TERM 1865
kill -SIGTERM 1865
kill -s 15 1865
kill -s TERM 1865
kill -s SIGTERM 1865

killは-s シグナル名(シグナルID)
kill -シグナル名(シグナルID)

CONT
18
停止したプロセスを再開。

STOP
19
プロセス停止。補足できない、ctrl + zではない。

TSTP
20
プロセス停止。補足できる、ctrl + z。

killコマンドは指定したプロセスにシグナルを送るコマンド。
kill プロセスID
シグナルを指定する場合は-sか-を付ける。
指定しない場合はTERMを送る。
TERMのIDは15。終了依頼。プロセスを後始末できる。後始末しないのはKILL。SIGKILL、9。

pkillとkillallの違い。
kill同様、プロセスにシグナルを送る。違いはプロセスの指定方法。

killはPID。
pkillはプロセス名や条件で探す。
pkill nginx
これだと、nginxという名前のプロセスを探し、見つかったすべてにTERMを送る。
pkillのオプション、-uでプロセスの実行ユーザーを指定できる。
-gでプロセスの実行グループを指定できる。

killall
指定した名前のプロセス全部に送る。
killall nginx
だと、名前がnginxのプロセスを全部探し、まとめてTERMを送る。

HUP  1  設定ファイルの再読み込み。
INT  2  実行中プログラムの中断。ctrl + c
KILL 9  プロセスを即終了。後始末できない。
TERM 15 プロセスの終了依頼。後始末可能
CONT 18 停止プロセスの再開。
STOP 19 プロセスの停止。補足できない。
TSTP 20 プロセスの停止。補足できる、ctrl + z。

psについて
a  ほかのユーザーのプロセスも表示
f  プロセスの親子関係をツリー状で。
u  プロセスを起動・所有しているユーザー名も表示
x  制御端末のないデーモンなどのプロセスを表示
-e すべてのプロセス
-f プロセス情報をフルフォーマットで表示
-l 詳細な情報を表示、親プロセスのPIDやnice値も併せて表示 -を付けないオプションもある。表示が若干異なる。
-p 指定したPIDのプロセス情報のみ表示

全プロセスを表示
ps -e

詳細表示
ps -ef
ps aux

動作中プロセスの親子関係をツリー状で表示
ps axf
pstree

niceについて
デフォルトのnice値は0
niceコマンドを使ってコマンドを実行、プロセスを起動した場合、nice値は10。

複数の仮想端末を開くコマンド
tmux
screen

psコマンドを10秒ごとに繰り返し実行し、結果を表示したい。
watchコマンド
watch -n 秒数 コマンド
一定時間ごとにコマンドを繰り返す。
デフォルトは２秒。
１０秒ごとなら
watch -n 10 ~

PIDを検索したいときだけpgrepを使う。
pgrep プロセス名
-uでユーザー名を指定、-gでグループを指定。

topコマンド
端末を専有してプロセスを監視。

uptime

uname


現在実行中のプロセスから、rootユーザーが実行したプロセスのプロセスIDを検索したい。
-Uオプションはユーザー名やユーザー識別子を指定でき、その実行プロセスを検索可能
rootユーザーのUIDは0。

# プロセスのnice値を表示するコマンドは
nice値の確認
ps l
top

# PIDが500のプロセスのnice値を-20に変更したい。
renice -n -20 -p 500
-nは省略可能
-pも省略可能
-nは省略してもしなくても関係なくほかのオプションの前に指定しなければならない。

renice -20 500

niceコマンドの場合、-nオプションを省略する場合-をnice値の前につける。
だからマイナス値だったら--20とかになる。
nice --20 コマンド

# コマンド「nohup コマンド &」の正しい説明は次のうちどれか。
nohupコマンド
HUPシグナル、１シグナルを無視してコマンドを実行し続ける。
また、&はコマンドをバックグランドで実行する。
ゆえにnohup コマンド &は、ユーザーがログアウト後も、コマンドをバックグランドで実行し続ける。

# 現在バックグラウンドで動作しているジョブと一時停止中のジョブを表示するコマンドは
jobsコマンドは、現在バックグラウンドで動作しているジョブと一時停止中のジョブを表示。

pidを指定してpsコマンドを使いたい場合。
ps -p PIDもしくはps p PID
-pの-は省略可。

fgにジョブ番号を指定して、そのジョブをフォアグラウンド動作に切り替える。
ジョブ番号を指定しない場合、カレントジョブが変更される。
+がついてるジョブ。

# フォアグラウンドで実行しているプロセスに割り込みシグナルを送信し、プロセスを終了したい。
割り込みシグナルを送信してプロセスを終了。
→ctrl + c
→実行中プログラムの中断。

# プロセスの稼動状況を一定時間ごとに更新して表示するコマンドは次
一定時間ごとに稼働状況を更新して表示。
→top
デフォルトは5秒ごとに表示が更新。

コマンドを一定時間ごとに繰り返すのはwatch。
デフォルトは２秒。
１０秒ごとなら
watch -n 10 コマンド


killコマンドはPIDを指定するコマンド。